---
# Handlers da role monitoramento (Aula 4)
- name: Reiniciar node_exporter
  ansible.builtin.systemd:
    name: node_exporter
    state: restarted
    daemon_reload: true

- name: Reiniciar rsyslog
  ansible.builtin.service:
    name: rsyslog
    state: restarted

# ===========================================================================
# Inventário do Ansible GERADO pelo Terraform (não edite à mão).
# Formato INI — Aula 3 (slide 26) / Aula 6 (slide 10).
# ===========================================================================
[aws]
servidor-aws ansible_host=${public_ip} ansible_user=${ssh_user} ansible_ssh_private_key_file=${private_key_path}

[servidores:children]
aws

[servidores:vars]
ansible_python_interpreter=/usr/bin/python3

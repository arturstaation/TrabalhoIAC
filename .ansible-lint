# Configuração do ansible-lint (usada no CI)
profile: basic

exclude_paths:
  - .github/
  - terraform/
  - collections/

skip_list:
  - yaml[line-length]   # linhas longas são comuns em tasks
  - var-naming[no-role-prefix]
